// Lancements des différentes fonctions

/**** Mode classique *****/
//Utiliser soit les fonctions "fetchworksData()" soit la fonction "initGallery()"

//fetchWorksData()

//apiFetch() 

//displayGallery(gall)

//removeGalleryHTML()

//initGallery()

//initFilters()
document.addEventListener("DOMContentLoaded", () => {
    initGalleryFilters()
});


/**** Mode Edition *****/
// Login/Logout
//document.addEventListener('DOMContentLoaded', toggleLoginLogout);

// Affichage Bandeau "Mode edition"
//document.addEventListener('DOMContentLoaded', checkEditMode);

// Boutton "modifier"
//document.addEventListener('DOMContentLoaded', showEditButton);

// Cache les fitltres 
//document.addEventListener('DOMContentLoaded', hideFiltersInEditMode);

document.addEventListener("DOMContentLoaded", () => {
    initModeEdition()
});


